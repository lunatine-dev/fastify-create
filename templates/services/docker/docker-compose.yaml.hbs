services:
  {{name}}:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
    ports:
      - "127.0.0.1:${PORT}:${PORT}"
    volumes:
      - .:/app
      - /app/node_modules
      - .env:/app/.env:ro
    restart: unless-stopped
