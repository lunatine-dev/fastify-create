services:
  app:
    container_name: {{name}}
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - NODE_ENV=production
    ports:
      - "${PORT}:${PORT}"
    volumes:
      - .:/app
      - /app/node_modules
      - .env:/app/.env:ro
    restart: unless-stopped
